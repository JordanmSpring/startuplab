
angular.module('StartupLab').directive("slProblems", [ 'Problem', function(Problem) {
  return {
    restrict: 'E',
    templateUrl: '<%= asset_path("templates/problems/modal.html") %>',
    controller: 'ProblemsController',
    scope: {
      idea: '=ngModel',
      linkTitle: '@'
    },
    link: function(scope, element) {
      scope.modal = element.find('.modal');
      scope.modal.on('show.bs.modal', function() {
        Problem.all(scope.idea).success(function(data) {
          scope.idea.problems = data.problems
        });
      });
    }
  }
}]);
