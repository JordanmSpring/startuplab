
angular.module('StartupLab').directive("slFounders", [ 'Problem', function(Problem) {
  return {
    restrict: 'E',
    templateUrl: '<%= asset_path("templates/founders/modal.html") %>',
    controller: 'FoundersController',
    scope: {
      idea: '=ngModel',
      linkTitle: '@'
    },
    link: function(scope, element) {
      scope.modal = element.find('.modal');
      // TODO: Load founders every time
      /*scope.modal.on('show.bs.modal', function() {
        Founder.all(scope.idea).success(function(data) {
          scope.idea.problems = data.problems
        });
      });*/
    }
  }
}]);
