
angular.module('StartupLab')
.controller('StaticController', ['$scope', '$window', '$anchorScroll', 'Session', function($scope, $window, $anchorScroll, Session) {
  $scope.flashError = $window.gon.flashError;

  $scope.scrollTo = function(id) {
    $anchorScroll(id);
  }
}]);
