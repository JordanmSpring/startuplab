angular.module('StartupLab')
.controller('IdeasController', ['$scope', 'Idea', function($scope, Idea) {
  Idea.all().then(function(response) {
    console.log(response);
    $scope.ideas = response.data.ideas;
  });
}]);
