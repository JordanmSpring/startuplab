// TODO: Move these to a routes.js file
angular.module('StartupLab', ['ngRoute', 'ngResource'])
 .config(['$routeProvider', function($routeProvider) {
  $routeProvider
    .when('/ideas', {
      templateUrl: '<%= asset_path("templates/ideas/index.html") %>',
      controller: 'IdeasController'
    })
    .when('/ideas/new', {
      templateUrl: '<%= asset_path("templates/ideas/new.html") %>',
      controller: 'NewIdeaController'
    })
    .when('/ideas/:id', {
      templateUrl: '<%= asset_path("templates/ideas/show.html") %>',
      controller: 'ShowIdeaController'
    })
    .when('/sign-in', {
      templateUrl: '<%= asset_path("templates/sessions/new.html") %>',
      controller: 'SessionsController',
      controllerAs: 'session'
    })
    .when('/register', {
      templateUrl: '<%= asset_path("templates/users/new.html") %>',
      controller: 'RegistrationsController'
    })
    .when('/', { redirectTo: '/ideas' })
    .otherwise({
      redirectTo: '/ideas'
    });
 }])
 .config([
   "$httpProvider", function($httpProvider) {
     $httpProvider.defaults.headers.common['X-CSRF-Token'] = $('meta[name=csrf-token]').attr('content');
   }
 ]);
