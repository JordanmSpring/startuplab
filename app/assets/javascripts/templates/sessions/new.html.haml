#u645-4.clearfix.colelem
  / content
  %p Sign In

  %form(name="sessionForm" ng-submit="login()" novalidate)
    / TODO: Validation
    %input.form-control(type="email" ng-model="session.email" required)
    %input.form-control(type="password" ng-model="session.password" required)
    %button.btn.btn-primary(type="Submit") Login
  
